server:
  port: 8083
spring:
  application:
    name: PromotionManagement
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5435/promotionManagement
    username: GorgeousSandwich
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  graphql:
    graphiql:
      enabled: true
