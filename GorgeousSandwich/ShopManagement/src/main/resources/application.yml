server:
  port: 8081
spring:
  application:
    name: ShopManagement
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5433/shopManagement
    username: GorgeousSandwich
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  graphql:
    graphiql:
      enabled: true
